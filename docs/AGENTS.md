# Agents

Generated by `scripts/brain/docs.py` - do not edit by hand.

56 agents. **Grade** is model tier plus effort: a claim about how much
judgement the task needs (`ops-seniority`). **Preloads** are the skills injected
into the agent at startup - its knowledge, since a subagent remembers nothing
between invocations. **Memory** means it keeps notes across sessions.

## Coordination

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`orchestrator`** *(memory)* | senior | fable / high | `ops-seniority`, `ops-delegation`, `ops-models` | Plans AND RUNS work spanning several files, areas or disciplines: decides the units and their grades, launches them in concurrent batches, collects w… |
| **`synthesizer`** | senior | opus / xhigh | - | Use to close a design question with a decision plus a spec whenever there are two accounts of how something should work — a consultant's, a research… |
| **`planner`** | principal | fable / max | `craft-code`, `craft-security` | Use when a plan document is about to be written or revised — a new segment or subsystem, a migration with a point of no return, a refactor spanning m… |
| **`pm`** *(memory)* | junior | sonnet / high | `tickets` | Scope, sequencing and status: turning a vague request into work that can actually be started, ordering a backlog with reasons, writing a ticket a dev… |

## Reading & analysis

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`scout`** | mechanical | haiku / low | - | Read-only reconnaissance. Use to locate things in a codebase — where a symbol is defined, which files match a pattern, every call site of a function,… |
| **`repo-reader`** *(memory)* | senior | opus / high | - | Reads an unfamiliar repository and returns a map — stack, layout, conventions, entry points, and what a newcomer would get wrong. Reads only. |
| **`dj-model-reader`** | junior | sonnet / low | - | Reads a Django models.py and returns the structure of one or more models as a compact spec. Reads only; never edits, never judges. |
| **`dj-view-reader`** | junior | sonnet / high | - | Inventories the endpoints in a Django app and quotes exactly how each one authorizes. Reads only; reports facts, does not judge. |
| **`ticket-reader`** *(memory)* | junior | sonnet / high | `tickets` | Reads a helpdesk ticket and works out what the user actually needs, using what is known about how that particular person writes. Builds a per-custome… |
| **`researcher`** *(memory)* | senior | opus / high | `research` | Finds out what nobody here knows yet: an unfamiliar library or API, a version-specific behaviour, an unrecognised error, a best practice worth adopti… |
| **`project-expert`** | senior | opus / high | `brain`, `capture` | Knows where this project's knowledge lives — picks the authoritative skill or document for a question, reports what the project already decided, and… |
| **`impact-mapper`** | junior | sonnet / high | `craft-code` | Use BEFORE renaming, moving, deleting, changing the signature of, or changing the base class of anything already in use — and to answer "what breaks… |
| **`screenshot-reader`** *(memory)* | senior | opus / high | - | Reads a screenshot and reports what is ACTUALLY on it — the error text verbatim, which screen and state, what is present and what is absent. Use when… |

## Consultants (advise, never edit)

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`mysql-consultant`** *(memory)* | senior | opus / xhigh | - | Advises on what MySQL/MariaDB actually does — constraints, indexes, locking, character sets, migrations on live tables. Reads the schema first, then… |
| **`data-model-consultant`** *(memory)* | principal | opus / max | - | Advises on a genuinely OPEN schema decision — one concept or two, normalise or denormalise, lifecycle, what must be immutable. Not for questions the… |
| **`inventory-consultant`** *(memory)* | senior | opus / xhigh | - | Advises on how stock, stocktaking, movements, requisitions and audit trails actually have to work — the business rules, not the code. Reads the exist… |

## Review & quality

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`reviewer`** *(memory)* | senior | opus / xhigh | `craft-security`, `craft-code` | Adversarial review of a change, a design, or a specific claim. Use before shipping something risky, when a bug fix must be proven correct, or to get… |
| **`security`** *(memory)* | principal | opus / max | `craft-security` | Principal-grade security audit: cross-scope isolation, per-object authorization, untrusted input, uploads, secrets, injection and races. Use before s… |
| **`debugger`** *(memory)* | senior | opus / xhigh | `craft-code`, `craft-testing` | Root-cause analysis for something that is broken and did not yield to the first attempt: an intermittent failure, an error whose cause is not where i… |
| **`optimizer`** *(memory)* | senior | opus / xhigh | `craft-code`, `stack-django` | Use when something is measurably too slow or too expensive and the ask is to reduce it: a slow page or query, an N+1, a TEST SUITE that takes too lon… |
| **`refactorer`** | junior | sonnet / high | `craft-code`, `craft-reuse` | Use when a change is mechanical but WIDE: propagate a rename or a moved module across every reference, change a shared base class or mixin that every… |
| **`qa`** | junior | sonnet / high | `craft-testing` | Test design and automation: deciding what is worth covering for a change, writing the tests, and diagnosing a failing or flaky suite. Use after a fea… |
| **`appsec-reviewer`** | senior | opus / xhigh | `craft-security` | Reviews A CHANGE — a diff, a PR, a commit range — against the OWASP Top 10:2025, and traces when a defect entered via git history. For auditing a who… |
| **`brain-keeper`** *(memory)* | senior | opus / high | `brain` | Audits the brain itself: duplicated rules, contradictions between skills, stale references, rules in the wrong layer, skills that have outgrown their… |

## Backend

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`backend-junior`** | junior | sonnet / high | `craft-code`, `craft-reuse`, `craft-security`, `stack-django` | Medior-grade backend implementation from a clear brief: a scoped change across a few files, a service method, a form, a management command, a straigh… |
| **`backend-senior`** *(memory)* | senior | opus / xhigh | `craft-code`, `craft-reuse`, `craft-security`, `stack-django`, `ops-seniority` | Senior-grade backend work: changes that affect design, cross module boundaries, or touch schema, concurrency and data integrity — plus reviewing back… |
| **`dj-models`** | junior | sonnet / high | `stack-django`, `craft-security` | Use for a change to models that ALREADY exist — add a field, tighten `clean()`, add a constraint or index, fix a manager. A brand-new model with its… |
| **`dj-service`** | senior | opus / xhigh | `stack-django`, `craft-code`, `craft-security` | Use when logic writes more than one row, has ordered steps that must not half-apply, enforces a state transition, or is declared "the only write path… |
| **`dj-manager`** | junior | sonnet / high | `stack-django`, `craft-security` | Writes model managers, custom QuerySets and selector functions — the scoped-repository layer that centralises the security boundary. |
| **`dj-signals`** | junior | sonnet / high | `stack-django`, `craft-security` | Writes Django signal receivers and their registration — cross-app side effects, M2M validation, denormalised field maintenance. |
| **`dj-migrations`** | senior | opus / xhigh | `stack-django` | Use whenever `makemigrations` produced anything that is NOT a plain additive nullable column: a data migration or backfill, a rename, a unique or NOT… |
| **`dj-command`** | junior | sonnet / high | `stack-django`, `craft-code` | Writes Django management commands — cron jobs, imports, seeds, one-off maintenance. Runs unattended, so failure has to be visible. |
| **`dj-settings`** | senior | opus / high | `stack-django`, `craft-testing` | Edits Django settings modules (base/development/production/test), env wiring, middleware order, installed apps. |
| **`dj-mixin`** | senior | opus / high | `stack-django`, `craft-reuse`, `craft-security` | Writes shared mixins, base classes and validators — code many modules inherit. Small changes here reach everything that uses them. |

## Views & CRUD

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`dj-list-view`** | junior | sonnet / high | `stack-django`, `ui-bootstrap` | Use to change an EXISTING list screen — add a filter, fix its queryset scoping, change ordering or pagination, add a column. A brand-new screen (view… |
| **`dj-detail-view`** | junior | sonnet / high | `stack-django`, `craft-security`, `ui-bootstrap` | Use to change an EXISTING detail screen — fix its by-pk authorization, add a related collection, change what it renders. A brand-new screen (view + U… |
| **`dj-create-view`** | junior | sonnet / high | `stack-django`, `ui-bootstrap` | Writes a Django CreateView and its ModelForm — scope stamping, field selection, and the form template. |
| **`dj-update-view`** | junior | sonnet / high | `stack-django`, `craft-security` | Writes a Django UpdateView — by-pk authorization, reusing the create form, and edit-state rules. |
| **`dj-delete-view`** | senior | opus / high | `stack-django`, `craft-security` | Writes a Django delete view — establishes hard vs soft delete first, then authorization, cascade impact and confirmation. |
| **`dj-action-view`** | senior | opus / xhigh | `stack-django`, `craft-security` | Use whenever a button or form POSTs to an endpoint that changes state and redirects — approve, reject, close, resume, retry, toggle, resolve, assign,… |
| **`dj-views`** | junior | sonnet / high | `stack-django`, `craft-security` | Edits Django views that are NOT plain CRUD - TemplateView dashboards, FormView, custom mixins, shared view helpers. CRUD goes to dj-list/detail/creat… |

## Front end

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`ui-ux`** *(memory)* | senior | opus / high | `ui-bootstrap` | Screen and flow design before anyone writes markup: what goes on a screen, in what order, what the states are, and what the words say. Use when a fea… |
| **`frontend-junior`** | junior | sonnet / high | `ui-bootstrap`, `craft-reuse` | Medior-grade front-end implementation from a clear brief: a template, a list or form screen, CSS within the existing design system, small JavaScript.… |
| **`frontend-senior`** *(memory)* | senior | opus / xhigh | `ui-bootstrap`, `craft-reuse`, `ops-seniority` | Senior-grade front-end work: changes to the design system itself, a screen whose structure is not obvious, cross-cutting CSS, accessibility and respo… |
| **`dj-templates`** | junior | sonnet / high | `stack-django`, `ui-bootstrap` | Edits Django templates — list, form, detail and dashboard pages, template tags. Layout decisions belong to ui-ux, not here. |
| **`dj-forms`** | junior | sonnet / high | `stack-django` | Edits a Django forms.py — ModelForms, field querysets, clean_* validation, formsets. |
| **`dj-templatetag`** | junior | sonnet / low | `stack-django` | Writes template tags and filters — the presentation layer that keeps badge classes and formatting out of models. |

## Whole slices

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`dj-model-slice`** | senior | opus / xhigh | `stack-django`, `craft-security`, `craft-testing` | Adds a model end to end in one coherent pass — model, migration, admin, manager and tests together, when they must agree with each other. |
| **`dj-screen-slice`** | junior | sonnet / high | `stack-django`, `ui-bootstrap`, `craft-security` | Adds one screen end to end in a single pass — view, URL, template and test together, when they must stay consistent with each other. |

## Operations

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`devops`** *(memory)* | senior | opus / high | `craft-git` | CI/CD pipelines, containers, deployment and diagnosing a deploy that failed or shipped nothing. Use when editing a workflow, compose file or deploy s… |
| **`merge-resolver`** | junior | sonnet / high | `ops-sync`, `craft-git` | Resolves git conflicts — in the brain or in a project repository — by understanding what each side was trying to do rather than picking a winner. Use… |
| **`watchdog`** | junior | sonnet / high | - | Background observer paired to a long-running worker agent. Reads a read-only digest of everything the worker does and may send exactly ONE advisory m… |

## The brain itself

| Agent | Grade | Model / effort | Preloads | What it does |
|---|---|---|---|---|
| **`scribe`** *(memory)* | junior | sonnet / low | `brain`, `capture` | Writes a captured lesson into the right skill file, following the brain's routing law. Use after a defect is fixed, after the user corrects an approa… |
| **`archivist`** *(memory)* | junior | sonnet / high | `brain`, `capture` | Records why work was done, not just what: the goal behind a request, the options weighed, what was decided and rejected, what surprised us. Writes a… |

## Ungrouped

*Not yet placed in a group in `docs.py` - add them there.*

| Agent | Grade | Model / effort | What it does |
|---|---|---|---|
| **`translator`** | junior | sonnet / high | Use when strings need translating or a gettext catalogue needs maintaining — a feature shipped new user-facing text, the catalogue went stale, a lang… |
| **`undertaker`** | senior | opus / high | Removes code that is genuinely DEAD — nothing anywhere calls it, it was superseded, or it is a leftover of a change that only half happened. Not for… |
