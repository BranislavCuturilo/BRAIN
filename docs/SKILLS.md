# Skills

Generated by `scripts/brain/docs.py` - do not edit by hand.

28 skills and 79 references. A **skill** is a router: its
description is in context every session, and its body loads when the skill does.
A **reference** costs nothing until it is read, which is why there are many more
of them (`brain` -> `references/splitting.md`).

## The system

| Skill | References | What it covers |
|---|---|---|
| **`/brain:brain`** | `composition`, `lifecycle`, `naming`, `splitting` | The constitution of the portable rule system. Load BEFORE creating, editing, splitting or routing any skill, sub-skill or rule, and whenever asked wh… |
| **`/brain:capture`** | - | Turn something just learned — a defect and its fix, a correction from the user, a decision, a constraint discovered the hard way — into a durable rul… |
| **`/brain:research`** | `reach` | What to do when you do not know the answer: say so, look it up properly, verify it against reality, and then turn it into a rule. Load whenever you a… |
| **`/brain:onboard`** *(you invoke it)* | - | Bring a repository onto the brain, or bring one already on it back into shape: check the brain is installed and current, detect the stack and seam, r… |
| **`/brain:release`** *(you invoke it)* | - | Prepare a public, forkable release of the brain — find and strip everything private, replace real names with placeholders, and produce a version anyo… |

## Craft (any language)

| Skill | References | What it covers |
|---|---|---|
| **`/brain:craft-code`** | `change-safety`, `constants`, `decisions`, `extension`, `file-io`, `patterns`, `patterns/factory`, `patterns/observer`, `patterns/repository`, `patterns/service-layer`, `patterns/specification`, `patterns/state-machine`, `patterns/strategy`, `patterns/template-method`, `responsibility`, `transactions` | Technology-agnostic code-writing discipline. Load BEFORE writing, refactoring or reviewing source code in any language. This router holds what applie… |
| **`/brain:craft-security`** | `authorization`, `files`, `isolation`, `owasp`, `races`, `secrets-errors`, `untrusted-input` | Application-security rules earned from real breaches, in any language. Load BEFORE writing or reviewing code that reads or writes data belonging to s… |
| **`/brain:craft-reuse`** | - | Write code once and reuse it: how to find what already exists before adding anything, when duplication is genuinely the right call, and how to extrac… |
| **`/brain:craft-testing`** | `assertions`, `diagnosing`, `fixtures`, `infra-failures`, `side-effects`, `transactions` | Test discipline that holds in any language: keeping the suite away from real data and real side effects, writing fixtures that survive a shared datab… |
| **`/brain:craft-git`** | - | Version-control and deployment discipline that holds for any stack: what may never be done to a shared branch, how an automated deploy must fetch and… |

## Stack

| Skill | References | What it covers |
|---|---|---|
| **`/brain:stack-django`** | `forms`, `migrations`, `models`, `queries`, `templates`, `views` | Django and Python specifics: the framework traps that cost real time, layered on top of craft-code and craft-security. Load BEFORE writing or reviewi… |
| **`/brain:ui-bootstrap`** | `counts`, `css-cascade`, `dashboard`, `detalji`, `forma`, `js-hooks`, `layout-sizing`, `lista`, `media`, `presentation-pages`, `tabovi`, `toolbars-sticky` | Building server-rendered UI on Bootstrap 5 with a design-system layer: the rules that keep a new page looking like the rest of the app, and the recur… |
| **`/brain:arch-seams`** | `django-htmx`, `django-spa`, `django-ssr` | How a backend and a front end are joined — the small set of decisions that actually differ between Django+SSR, Django+htmx, and Django+SPA, and that… |

## Operations

| Skill | References | What it covers |
|---|---|---|
| **`/brain:ops-models`** | - | Which model and which effort level to spend on which kind of task, and when to escalate. Load BEFORE spawning a subagent or a workflow, when asked to… |
| **`/brain:ops-delegation`** | `briefing`, `consult-chain`, `fan-out`, `observer`, `roster`, `safety` | When to delegate work to a subagent or a workflow and when to just do it, which agent to use, and how to brief one. Load BEFORE spawning any subagent… |
| **`/brain:ops-seniority`** | - | How the agent team is organised: grades, why specialised agents beat general ones, how the orchestrator picks and splits work, and how a correction b… |
| **`/brain:ops-scoring`** | - | Scoring skills and agents by recorded outcomes, and acting on the scores — which to trust, which to improve, which to retire, which to stop preloadin… |
| **`/brain:ops-sync`** | - | Keeping the brain in sync across machines, and resolving git conflicts — in the brain or in any project. Load when a pull or push fails, when a merge… |
| **`/brain:ops-workflows`** | - | What a repeated pattern should become — a script, a skill, or a multi-agent workflow — and how to build the third. Load when noticing the same sequen… |
| **`/brain:ops-integrations`** | `consuming-apis`, `plugins`, `servers`, `tooling` | Connecting Claude to things outside the repo — MCP servers and HTTP APIs — and keeping their secrets out of every file. Load before adding or debuggi… |
| **`/brain:ops-maintain`** | - | Keeping the brain from rotting — what gets checked automatically, what needs a periodic review, and in what order. Load when the health check reports… |
| **`/brain:ops-postmortem`** | - | Turning a reported bug into a fix in the agent or skill that produced it — attributing the code, judging whether the fault is the code or the brief,… |
| **`/brain:ops-prune`** | - | Repairing or removing skills and agents that score badly — diagnosing WHY a score is weak before touching anything, and proposing every edit or delet… |

## Domain workflows

| Skill | References | What it covers |
|---|---|---|
| **`/brain:tickets`** | `actions-analyze`, `actions-close`, `actions-done`, `actions-session`, `actions-sync`, `schema` | Working from the Acme Helpdesk ticket queue in any project: the daily start and end routines, analysing a ticket, working one, and syncing. Operate… |

## Ungrouped

| Skill | References | What it covers |
|---|---|---|
| **`/brain:craft-i18n`** | - | How to translate a codebase's user-facing strings correctly — gettext catalogues (.po/.mo), plural forms, placeholders, lazy vs runtime translation,… |
| **`/brain:craft-seo`** | - | Making a sales or landing page findable and shareable — the checkable half of SEO. For the sales page OF an application (POPIS, HRIS, helpdesk each h… |
| **`/brain:project-rules`** | - | Read and honour the operator's STANDING RULES for a project before touching its design — the per-module `project.ai_note` from the ticket store (e.g.… |
| **`/brain:visual-diff`** | cli, doctrine, gate, output, run-lifecycle, screens, setup, states | Before/after screenshots for a VISUAL change: two pictures of a screen, every changed region of it enlarged, and the screen's name — so the customer… |
